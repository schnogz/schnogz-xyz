version: 1.0
frontend:
  phases:
    preBuild:
      commands:
        - 'corepack enable'
        - 'yarn install --immutable'
    build:
      commands:
        - 'yarn ci:build'
  artifacts:
    baseDirectory: '/public'
    files:
      - '**/*'

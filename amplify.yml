version: 1.0
frontend:
  phases:
  preBuild:
    commands:
      - 'yarn'
    build:
      commands:
        - 'yarn gulp build'
  artifacts:
    files:
      - '**/*'
  discard-paths: yes
  baseDirectory: 'dist'

version: 1.0
frontend:
  phases:
    preBuild:
      commands:
        - 'yarn'
    build:
      commands:
        - 'yarn ci:build'
  artifacts:
    baseDirectory: '/public'
    files:
      - '**/*'
